import { FormatCountsPipe } from './format-counts-pipe';

describe('FormatCountsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCountsPipe();
    expect(pipe).toBeTruthy();
  });
});
